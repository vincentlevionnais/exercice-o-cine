{% extends 'base.html.twig' %}

{% block title %}Job{% endblock %}

{% block menu_active %}active{% endblock %}
{% block menu_logout %}out-alt{% endblock %}

{% block body %}
    <h1>Job</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_job_index') }}">back to list</a>

    <a href="{{ path('back_job_edit', {'id': job.id}) }}">edit</a>

    {{ include('back/job/_delete_form.html.twig') }}
{% endblock %}
