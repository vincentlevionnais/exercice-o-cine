{% extends 'base.html.twig' %}

{% block title %}O'Ciné{% endblock %}

{% block body %}

    <div class="title">
        <h2>All Movies <i class="fas fa-film"></i></h2>
    </div>  

    <div class ="container">
        {% for movie in movies %}
            <a href="{{path('movie_read', {slug: movie.slug}) }}">
                <div class = "card" >
                    <h3>{{movie.title}}</h3>
                </div>
            </a>
        {% endfor %}
    </div>
    

{% endblock %}
