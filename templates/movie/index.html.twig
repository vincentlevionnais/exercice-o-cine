{% extends 'base.html.twig' %}

{% block title %}O'Ciné{% endblock %}

{% block body %}

    <div class="title">
        <h2>Movies</h2>
    </div>  

    <div class ="container">
        {% for movie in movies %}
            <div class = "card" >
            <a href="{{path('movie_read', {id: movie.id}) }}">
            <h2>{{movie.title}}</h2>
            </a></div>
        {% endfor %}
    </div>
    
{% endblock %}
